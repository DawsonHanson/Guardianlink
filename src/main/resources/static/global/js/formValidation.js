// to be used in forms that only need color change effect
const submit = document.getElementById('Submit')

submit.addEventListener('click', () => {
  document.getElementById('Form').className='submitted'
});